- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.fonts:
    ~/.wallpaper:
    ~/.scripts:
    ~/.config/i3: i3
    ~/.config/polybar: polybar
    ~/.config/rofi: rofi
    ~/.config/kitty: kitty
    ~/.config/Code - OSS/User/settings.json: code/User/settings.json

- create:
  - ~/documents
  - ~/images
  - ~/projets
  - ~/téléchargements

- shell:
  - [git submodule update --init --recursive, Installing submodules]
